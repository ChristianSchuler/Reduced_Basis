 P = zeros(2,2,2);
 
 a = [1 1; 0 0];
 b = [0 0; 2 2];
 P(:,:,1) = a;
 P(:,:,2) = b;
 
 
 P2 = zeros(1,2,2);
 c = [1 1];
 c = sparse(c);
 P2(:,:,1) = c;
 P2(:,:,2) = c;
 s = mtimesx(c,P);
 
 z = [0;0];
 P = [P; z];
 
 d = [1  1; 1 1];
 d = [d z];
 
